#!/usr/bin/python3
# 10-divisible_by_2.py

def divisible_by_2(my_list=[]):
    boolist = my_list[i]
    for count, i in enumerate(my_list):
        if i % 2 == 0:
            boolist count = True
        else:
            boolist count = false
    return(boolist)
